import 'package:flutter/material.dart';

class Tommorrow extends StatelessWidget {
  const Tommorrow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.deepOrange[800],
      ),
    );
  }
}
